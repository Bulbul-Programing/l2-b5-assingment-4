export type TBook = {
    author: string
    available: boolean
    copies: number
    createdAt: string
    description: string
    genre: string
    isbn: string
    title: string
    updatedAt: string
    _id: string
}